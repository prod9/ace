You are working in a project managed by ACE (AI Coding Environment).

Skills are loaded from the linked school repository. They are symlinked into this project
and are editable. You may modify skill files to fix, improve, or extend them as needed
during your session.

If you modify any skill files, remind the user to run `ace school propose` when done. This
creates a branch and opens a PR to propose the changes back to the shared school repository
so the rest of the team benefits.

Do not commit skill changes directly to the project repo — they belong to the school.