The school clone has uncommitted local changes. This may block automatic updates.

If the changes are intentional (skill edits, new skills), propose them back to the school repo.
If the changes are just artifacts (e.g. `__pycache__/`, `.pyc`, `.DS_Store`), add a `.gitignore`
entry to the school repo so they stop showing up as dirty.
